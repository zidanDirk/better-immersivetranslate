# 翻译诊断日志写入固定下载子目录

项目决定在用户明确开启后，通过 Chrome Downloads API 将翻译诊断日志写入系统下载目录的 `better-immersivetranslate/logs/` 子目录，并在产生首个日志后于设置页显示解析出的实际路径。相比需要反复确认授权且不暴露绝对路径的 File System Access API，以及显著增加安装和维护成本的本机辅助程序，固定下载子目录能让后台翻译失败稳定落盘并保持现有纯浏览器扩展的发行方式；代价是用户不能自由选择目录，而且每个最终失败批次需要保存为独立文件。
